services:
  cauliflower-app:
    image: cauliflower
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SOCKET_FIREWALL_KEY: ${SOCKET_FIREWALL_KEY}
    env_file:
      - .env
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
